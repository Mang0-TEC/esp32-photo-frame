# PlatformIO
.pio/
.cache
.dummy
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
compile_commands.json

# Secretos — el diseño dice que no debe haber ninguno, pero por si acaso
secrets.h
wifi_credentials.h

# Binarios
*.bin
*.elf

# La página gzipeada a array de C: la genera tools/empaquetar_pagina.py en cada
# build. Son ~183 KB de fuente que cambian ENTEROS al tocar una línea del HTML, y
# versionarlos ensuciaría todos los diffs de la página. El script sí se versiona,
# así que el repo basta para flashear.
firmware/src/pagina.h

# macOS
.DS_Store

# CAD
*.f3d
*.stl.bak